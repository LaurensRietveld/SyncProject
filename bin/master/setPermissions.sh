#!/bin/sh
sudo chown -R lrd900:syncProject /usr/local/share/syncProject
sudo chmod -R 775 /usr/local/share/syncProject
sudo chmod +x /usr/local/share/sesameExport.jar
sudo chmod +x /usr/local/share/daemon.jar